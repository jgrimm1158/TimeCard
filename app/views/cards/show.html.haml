%h1= "Time Card for week starting #{@card.week_starting.strftime("%B %d, %Y")}" 
- @disabled = @card.is_submitted  
= render :partial => 'form'

= link_to 'Show all', :action => 'show', :controller => 'users', :id => current_user.login